name: HelloWorld
on: workflow_dispatch
jobs:
  Hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: |
          echo "Welcome to GitHub Actions"
          echo "Multiple Commands in a single step"
